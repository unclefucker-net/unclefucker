


class Application {

	// this is a placeholder for the application

	constructor() {
		// load the stream
		var stream = new Stream('#stream');
		
	}

}

// Load the application once the DOM is ready, using jQuery.ready:
$(() => {
    // start the application
    new Application();
});